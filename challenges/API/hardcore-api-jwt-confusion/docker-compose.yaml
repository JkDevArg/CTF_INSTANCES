version: '3.8'

services:
  hardcore-api-jwt-confusion:
    build: .
    ports:
      - "${PORT_80:-8082}:80"
    environment:
      - FLAG=${FLAG}
    mem_limit: 256m
    cpus: 0.5
    restart: unless-stopped
