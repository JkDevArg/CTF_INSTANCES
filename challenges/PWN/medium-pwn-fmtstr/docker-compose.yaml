version: '3.8'

services:
  pwn:
    build: .
    ports:
      - "${PORT_9999:-9999}:9999"
      - "${PORT_80:-8080}:80"
    environment:
      - FLAG=${FLAG}
    restart: unless-stopped
    mem_limit: 128m
    cpus: 0.5
    security_opt:
      - no-new-privileges:true
