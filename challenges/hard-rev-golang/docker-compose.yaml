version: '3.8'

services:
  rev:
    build: .
    ports:
      - "${PORT_80:-8080}:80"
    environment:
      - FLAG=${FLAG}
    restart: unless-stopped
    mem_limit: 256m
    cpus: 0.5
    security_opt:
      - no-new-privileges:true
