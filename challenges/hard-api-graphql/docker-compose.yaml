version: '3.8'

services:
  hard-api-graphql:
    build: .
    ports:
      - "${PORT_80:-8080}:80"
    environment:
      - FLAG=${FLAG}
    mem_limit: 256m
    cpus: 0.5
    restart: unless-stopped
